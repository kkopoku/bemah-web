# Example environment variables for local development
# Copy this file to .env.local and fill in real values (do NOT commit secrets).

# Backend API base URL (default for local dev)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Public client ID for OAuth / frontend integration
NEXT_PUBLIC_CLIENT_ID=your-client-id

# Client secret 
NEXT_PUBLIC_CLIENT_SECRET=your-client-secret

# Auth secret used by Auth.js (generated by `npx auth`)
AUTH_SECRET=your-auth-secret

# Notes:
# - Rename to .env.local for local development.